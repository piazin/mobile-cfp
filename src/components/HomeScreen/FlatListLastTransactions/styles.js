import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: "100%",
    flexDirection: "row",
    alignItems: "center",
    marginVertical: 8,
    marginTop: 15,
    position: "relative",
  },

  iconType: {
    position: "absolute",
    right: 115,
  },
});

export default styles;
